# 2026-04-30 — Session Notes

## Parent Messages Page — Implemented

Built `parent_messages.html` as a standalone parent-to-teacher messaging page.

**Conversations shown:**
- Ms. Sarah (Piano)
- Mr. Chen (Math)
- Ms. Amy (English)

**Conversation window behavior:**
- Click a conversation → chat window opens with back button in nav
- Back button returns to conversation list
- Nav shows teacher name + avatar during chat
- Hamburger hidden during chat view
- Nav-right (avatar + lang selector) hidden during chat
- Message history includes both sent and received messages with timestamps

**Nav bar specifics:**
- `nav { display: flex; justify-content: space-between; align-items: center; height: 60px; padding: 0 24px; }`
- `.nav-left { display: flex; align-items: center; gap: 12px; }` (hamburger/Logo beside each other)
- Back button styled as `←` text with gold color, cursor pointer

---

## Student Profile Page — URL Parameter Support

`student_profile.html` now reads `?student=` URL parameter to display the correct child.

| Parameter | Child | Avatar | Subjects | Level |
|---|---|---|---|---|
| `?student=jw` | Jasper Wu | JW (blue `#3b82f6`) | Piano, Math | Form 2, Intermediate |
| `?student=lw` | Lily Wu | LW (pink `#ec4899`) | English, Art | Grade 1, Beginner |

Each student's profile also includes DOB and Goals fields.

---

## Parent Schedule — Calendar Updates

Calendar dots now show three states:
- 🟢 Green = confirmed
- 🟡 Gold = pending
- 🔴 Red = cancelled

Cancelled lessons are included in the calendar view.

---

## Navigation — Cleanup Across All Parent Pages

**Removed from all hamburger menus:** Children, Profile, Settings (leaving just Dashboard, My Schedule, Messages)

**Hamburger → Back button on student_profile.html:** Back button links to `parent_dashboard.html`. Nav-left shows back button + Minerva logo side by side.

**Active gold highlight fix:** Query strings like `?v=ps001` in nav hrefs broke path comparison — resolved by `.split('?')[0]` stripping before compare.

**Nav bar consistency across parent pages:**
- `display: flex; justify-content: space-between; align-items: center;` on `<nav>`
- `.nav-left` flex container for hamburger + logo
- Avatar + lang selector on far right via `nav-right`
- Height: 60px, padding: 0 24px

---

## Button Styles (Consistent Across Pages)

Used across parent_profile, student_profile, and parent_messages:
- **Save Changes:** `.btn-primary` — navy background, white text
- **Delete Account:** `.btn-danger` — red outline/background

---

## Files in Scope

**Parent pages (all live):**
- `parent_dashboard.html`
- `parent_schedule.html`
- `parent_messages.html`
- `student_profile.html`
- `parent_profile.html`

**Reference pages:**
- `teacher_dashboard.html`
- `teacher_schedule.html`
- `messages.html` (teacher messages — NOT used by parent nav)

**External resources:**
- i18n.js for language switching
- Minerva GitHub Pages: `https://jonathancheezy.github.io/Minerva/`